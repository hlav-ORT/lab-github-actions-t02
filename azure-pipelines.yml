trigger:
  branches:
    include:
      - main

pool:
  vmImage: 'ubuntu-latest'

stages:
  - stage: Build
    displayName: 'Build imagen Docker'
    jobs:
      - job: BuildImage
        displayName: 'Build imagen'
        steps:
          - script: |
              docker build -t lab-github-actions:$(Build.SourceVersion) .
            displayName: 'Docker build'
